https://codepen.io/rbhouck32/pen/OJJzpQN



    1. What is Semantic HTML? Semanctic html helps give html meaning.
    2. What is HTML used for? It is used to provide markup for electronic documents to be displayed on the world Wide Web
    3. What is an attribute and where do we put it? an attribute gives additional information to a tag. we put it within a tag using (i.e. class= "classisanattribute") 
    4. What is the h1 tag used for? How many times should I use it on a page? h1 tag stands for header. Typically is Used once per page
    5. Name two tags that have required attributes <input> and <form>
    6. What do we put in the head of our HTML document? typically, your h1 your nav and perhaps a tagline.
    7. What is an id? an id is an html attribute that gives a UNIQUE specificity to a tag.
    8. What elements can I add an id to? it can be added to any element
    9. How many times can I use the same id on a page? only once
    10. What is a class? class is an attribute that specifies one or more class names to an element.
    11. What elements can I add a class to? any element.
    12. How many times can I use the same class on a page? multiple.... all classes with same names will inherit same CSS properties as said class
    13. How do I get my link to open in a new tab?right click open in new tab
    14. What is the alt attribute used for? allows for alternative text to be rendered. when the intial element properties are not available
    15. How do I reference an id? using the # symbol
    16. What is the difference between a section and a div. section is basically a div but gives semantic meaning to it. It allows you to break up content by telling the page it is a section.
    17. What is CSS used for? CSS is used to style your html markup. It is the way a web page looks. layout/design
    18. How to we select an element? Example - every h2 on the page h2{}
    19. What is the difference between a class and an id? - Give me an example of when I might use each one. class can be used multiple times within a html page.. Id is very specific used for one element only.
	examples: to style paragraphs in the about us section you may use class= "content-info" and then something like .content-info p {}/ maybe you have multiple h2 tags but want only one to have a unique style you may use <h2 id="unique"> (css= #unique{})
    20. How do we select classes in CSS? we use a period .
    21. How do we select a p element with a single class of “human””? .human p
    22. What is a parent child selector? When would this be useful? 
    23. How do you select all links within a div with the class of sidebar?
    24. What is a pseudo selector? is a selector that selects an element in a specific state live :hover
    25. What do we use the change the spacing between lines? line-height
    26. What do we use to change the spacing between letters?letter-spacing
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? transform: uppercase, transform: lowercase;, transform: capitalize;
    28. How do I add a 1px border around my div that is dotted and black? div { border: 1 px dotted black;}
    29. How do I select everything on the page? *{} is the universal selector
    30. How do I write a comment in CSS?/* */
    31. How do I find out what file I am in, when I am using the command line? cd
    32. Using the command line - how do I see a list of files/folders in my current folder? ls
    33. How do I remove a file via the command line? Why do I have to be careful with this? 
    34. Why should I use version control? 
    35. How often should I commit to github? 
    36. What is the command we would use to push our repo up to github? 
    37. Walk me through Lambda's git flow.      